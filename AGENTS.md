# Agents

This repository is optimized for **Cursor** with rules under `.cursor/rules/`.

- **Leader Key** behavior and macOS troubleshooting: `leader-key-macos.mdc`
- **Documentation tone** (Cursor Glass alignment): `cursor-glass.mdc`

Human-facing guides live in `docs/`. Run `python -m unittest discover -s tests -v` before pushing config changes.
